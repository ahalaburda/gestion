<%= form_for @parametro, :html => {:multipart => true} do |f| %>
<% if @parametro.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@parametro.errors.count, "error") %> prohibited this parametro from being saved:</h2>

    <ul>
    <% @parametro.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
  <div class="form-inputs">
    <%= f.label :nombre_empresa %>
    <%= f.text_field :nombre_empresa %>
    <div class="row">
      <div class="col-sm-6 col-md-4">
        <div class="thumbnail">
          <%= f.file_field :logo_empresa %>
        </div>
      </div>
    </div>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>