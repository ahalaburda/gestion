report.start_new_page do |page|
  page.item(:banco).value(@cheques_entrantes.nombre)
  page.item(:numero).value(@cheques_entrantes.numero)
  page.item(:fecha).value(@cheques_entrantes.fecha)
  page.item(:monto).value(@cheques_entrantes.monto)
  page.item(:concepto).value(@cheques_entrantes.concepto)
  page.item(:persona).value(@cheques_entrantes.persona)
end